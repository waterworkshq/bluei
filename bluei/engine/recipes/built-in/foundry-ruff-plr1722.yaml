id: foundry-ruff-plr1722
rule: ruff-plr1722
language: python
safety: needs_validation
description: Replace bare `exit()` with `sys.exit()` and ensure `sys` is imported
match:
  type: rule_exact
  rule: ruff-plr1722
  scope: line
replacement:
  type: regex_substitute
  pattern: exit\(1\)
  replacement: import sys\n\nsys.exit(1)
  count: 1
validation:
  run_baseline: true
  run_target: true
metadata:
  version: 1
  author: bluei-recipe-foundry
  source_pattern_id: seed-ruff-plr1722
  source_bundle_id: gb-ruff-plr1722
priority: 50
