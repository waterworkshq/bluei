id: foundry-ruff-b007
rule: ruff-b007
language: python
safety: needs_validation
description: Replace unused enumerate loop index with `_`
match:
  type: rule_exact
  rule: ruff-b007
  scope: line
replacement:
  type: regex_substitute
  pattern: for \w+, x in enumerate\(
  replacement: for _, x in enumerate\(
  count: 1
validation:
  run_baseline: true
  run_target: true
metadata:
  version: 1
  author: bluei-recipe-foundry
  source_pattern_id: seed-ruff-b007
  source_bundle_id: gb-ruff-b007
priority: 50
