"""bluei.engine — local sandbox QA workflow runner package.

Import directly from submodules::

    from bluei.engine.models import Finding
    from bluei.engine.constants import WORKSPACE
    from bluei.engine.lifecycle import apply_autofix

Symbol re-exports were removed to reduce import coupling and avoid
latent circular-import risk.  Access submodules explicitly.
"""
